window.DDD_API_CONFIG = Object.freeze({
  // Paste the deployed Google Apps Script /exec URL here when the pilot backend is ready.
  // Leave blank to keep the GitHub Pages site in local prototype mode.
  baseUrl: ""
});
