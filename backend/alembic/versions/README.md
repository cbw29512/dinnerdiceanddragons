# Alembic revisions

Production schema revisions live here.

The first real revision will create the durable Dinner, Dice & Dragons identity tables (`users` and `user_roles`). Do not create empty placeholder revisions merely to prove Alembic works; migration history should represent real schema changes.
